        # DESeq2 — BioMate KB Test Suite

        ## Package Overview
        - **Bioconductor**: [https://bioconductor.org/packages/DESeq2](https://bioconductor.org/packages/DESeq2)
        - **Software DOI**: N/A
        - **EDAM operations**: N/A
        - **Domains**: rna_seq, differential_expression
        - **Workflow phases**: analysis, clustering, differential, import, normalization, qc, visualization
        - **KB workflow steps**: 7

        ## Test Data
        Built-in DESeq2 example data (makeExampleDESeqDataSet)

        ## Test Files
        | File | Description |
        |------|-------------|
        | `test_workflow.R` | Main test script using Bioconductor sample data |
        | `expected_outputs.json` | Expected outputs for validation |
        | `test_data/` | Any required input data files |

        ## How to Run
        ```bash
        # Requires R with Bioconductor packages installed
        Rscript test_workflow.R
        ```
        Expected runtime: ~30 seconds

        ## Workflow Steps Tested
        - **Step 1**: Create DESeqDataSet from count matrix (`DESeqDataSetFromMatrix, DESeq, resultsNames`)
- **Step 2**: Filter by row sum threshold (`rowSums, estimateSizeFactors, estimateDispersions`)
- **Step 3**: Variance-stabilizing transformation (VST) (`vst, rlog, normTransform`)
- **Step 4**: Run DESeq2 differential expression (`DESeq, results, resultsNames`)
- **Step 5**: Compute distance matrix (`dist`)
- **Step 6**: MA plot of DE results (`plotMA, plotCounts, ggplot`)
- **Step 7**: Analysis analysis (`all, mcols, register`)

        ## Acceptance Criteria
        - Script exits with status 0
        - All `stopifnot()` checks pass
        - Output files are created (where applicable)
        - Results match `expected_outputs.json` structure

        ## Known Limitations
        - Some steps require optional large data packages
        - Interactive steps (plot display) are skipped
        - Full vignette workflows may require larger datasets

        ---
        *Generated by BioMate KB test generator — 2026-02-26*
