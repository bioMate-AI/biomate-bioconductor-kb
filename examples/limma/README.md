        # limma — BioMate KB Test Suite

        ## Package Overview
        - **Bioconductor**: [https://bioconductor.org/packages/limma](https://bioconductor.org/packages/limma)
        - **Software DOI**: N/A
        - **EDAM operations**: N/A
        - **Domains**: rna_seq, differential_expression, microarray
        - **Workflow phases**: analysis, differential, import, normalization, qc, visualization
        - **KB workflow steps**: 12

        ## Test Data
        Simulated microarray / RNA-seq data via limma-voom

        ## Test Files
        | File | Description |
        |------|-------------|
        | `test_workflow.R` | Main test script using Bioconductor sample data |
        | `expected_outputs.json` | Expected outputs for validation |
        | `test_data/` | Any required input data files |

        ## How to Run
        ```bash
        # Requires R with Bioconductor packages installed
        Rscript test_workflow.R
        ```
        Expected runtime: ~20 seconds

        ## Workflow Steps Tested
        - **Step 1**: Reading in count-data (`download.file, untar, gunzip`)
- **Step 2**: Organising sample information (`rep`)
- **Step 3**: Organising gene annotations (`select`)
- **Step 4**: Transformations from the raw-scale (`cpm`)
- **Step 5**: Removing genes that are lowly expressed (`rowSums, filterByExpr, brewer.pal`)
- **Step 6**: Normalising gene expression distributions (`calcNormFactors, cpm`)
- **Step 7**: Unsupervised clustering of samples (`cpm, levels, brewer.pal`)
- **Step 8**: Creating a design matrix and contrasts (`model.matrix, makeContrasts`)
- **Step 9**: Removing heteroscedascity from count data (`voom, lmFit, contrasts.fit`)
- **Step 10**: Examining the number of DE genes (`decideTests, treat, vennDiagram`)

        ## Acceptance Criteria
        - Script exits with status 0
        - All `stopifnot()` checks pass
        - Output files are created (where applicable)
        - Results match `expected_outputs.json` structure

        ## Known Limitations
        - Some steps require optional large data packages
        - Interactive steps (plot display) are skipped
        - Full vignette workflows may require larger datasets

        ---
        *Generated by BioMate KB test generator — 2026-02-26*
